'use strict';

describe('Service: firebase.utils', function () {

  // // load the service's module
  // beforeEach(module('firebase.utils'));
  //
  // // instantiate service
  // var firebase.utils;
  // beforeEach(inject(function (_firebase.utils_) {
  //   firebase.utils = _firebase.utils_;
  // }));
  //
  // it('should do something', function () {
  //   expect(!!firebase.utils).toBe(true);
  // });

});
